## Personal/Technical Blog built with Gatsby.js

### Running Locally
- Clone repository 
- Run `npm install` at the root of the project
- Run `gatsby develop` at the root of the project 
- Access website at `localhost:8000`
